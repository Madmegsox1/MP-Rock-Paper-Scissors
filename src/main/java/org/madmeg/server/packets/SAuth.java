package org.madmeg.server.packets;

import org.madmeg.networking.Packet;

public final class SAuth extends Packet {
    public SAuth() { // TODO database and rest of auth stuff
        super("SAuth");
    }
}
